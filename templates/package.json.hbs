{
  "name": "{{date}}",
  "title": "{{title}}",
  "author": { "name": "{{author}}" },
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "slidev build --out ../../dist/{{date}} --base $BASE_PATH/{{date}}",
    "dev": "slidev",
    "export": "slidev export --output ../../dist/{{date}}"
  }
}
